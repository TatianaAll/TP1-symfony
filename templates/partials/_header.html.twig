<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Articles en ligne</title>
    <link rel="stylesheet" href= {{ asset('asset/style/style.css') }}>
</head>

<body>
<header>
    <h1>Un site d'articles fantastiques</h1>
    <nav>
        <ul>
            {# Pour faire mes lien j'utilise le path + le nom indiqué dans mon routeur #}
            <li><a href= {{ path('home') }}>Accueil</a></li>
            <li><a href="{{ path('articles') }}">Articles</a></li>
            <li>
                {# je me crée un formulaire qui renvoie sur la page search articles avec path #}
                <form method="get" action="{{ path('articles_search_result') }}">
                    <input type="text" id="search" name="search" placeholder="Rechercher un article">
                    <button type="submit">Rechercher</button>
                </form>
            </li>
        </ul>
    </nav>


</header>